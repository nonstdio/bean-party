extends MinigameController


func _on_minigame_setup() -> void:
	# Read player, team, RNG, and input data from get_minigame_context().
	pass


func _on_minigame_start() -> void:
	# Run the round, then submit exactly one outcome-only MinigameResult.
	pass


func _on_minigame_abort(_reason: String) -> void:
	# Stop timers, audio, and other minigame-owned work.
	pass
